{% block main %}
    {% for card in deck %}
        <span>{{card}}</span>
        <div class="span8">
            <p>Kort: <strong>{{deckStr|length}}</strong></p>
        </div>
    {% endfor %}
{% endblock %}